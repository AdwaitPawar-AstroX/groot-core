pyyaml
requests
anthropic
python-dotenv

# Server
fastapi
uvicorn[standard]

# PC agent
websockets

# Voice client (run only where a mic/speaker exist — typically the server's own PC)
sounddevice
numpy
faster-whisper
openwakeword
piper-tts
